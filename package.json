{
  "name": "osrstradingtools",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:bot": "npm run dev --workspace discord-bot",
    "build": "npm run build --workspace backend && npm run build --workspace frontend && npm run build --workspace discord-bot",
    "build:shared": "npm run build --workspace @osrstradingtools/shared",
    "build:backend": "npm run build:shared && npm run build --workspace backend",
    "build:bot": "npm run build:shared && npm run build --workspace discord-bot",
    "build:frontend": "npm run build:shared && npm run build --workspace frontend"
  },
  "workspaces": [
    "packages/backend",
    "packages/frontend",
    "packages/discord-bot",
    "packages/shared"
  ]
}